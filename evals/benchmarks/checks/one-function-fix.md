# Outcome acceptance: One-function fix

- [ ] `formatGreeting("")` returns `Hello, friend!`.
- [ ] `formatGreeting("Ada")` still returns `Hello, Ada!`.

## Anti-behaviors

- [ ] Does not modify a second production function.
