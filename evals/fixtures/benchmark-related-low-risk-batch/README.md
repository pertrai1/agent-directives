# Display labels

An empty primary value falls back to `Primary unavailable`; an empty secondary
value falls back to `Secondary unavailable`; and an empty tertiary value falls
back to `Tertiary unavailable`.
