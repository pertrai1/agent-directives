---
"agent-directives": minor
---

Install the decision-log template during sync by adding template entries to the manifest and copying `templates/decision-log.md` to `.agents/templates/decision-log.md`. Update session-decision guidance to use the installed template path and clarify GitNexus usage should run through existing CLI/MCP tools without installing skills or changing agent instruction files.
